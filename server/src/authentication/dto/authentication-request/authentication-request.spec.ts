import { AuthenticationRequest } from './authentication-request';

describe('AuthenticationRequest', () => {
  it('should be defined', () => {
    expect(new AuthenticationRequest()).toBeDefined();
  });
});
