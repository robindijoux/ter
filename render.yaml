services:
  - type: web
    name: server
    env: docker
    region: frankfurt
    plan: free
    rootDir: server
    healthCheckPath: /
    dockerfilePath: server/Dockerfile
    envVars:
      - key: PORT
        value: 3000
